{
  "name": "financial-forensics-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run dev --prefix frontend",
    "start": "npm start --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  }
}
